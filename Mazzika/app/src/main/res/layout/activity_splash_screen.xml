<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorPrimarySurface"
    tools:context=".Activities.SplashScreenActivity">

    <ImageView
        android:id="@+id/image_splash"
        android:layout_width="@dimen/image_splash_dimen"
        android:layout_height="@dimen/image_splash_dimen"
        android:layout_centerInParent="true"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/text_splash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/image_splash"
        android:layout_centerHorizontal="true"
        android:fontFamily="sans-serif-condensed-medium"
        android:text="@string/app_name"
        android:textAllCaps="true"
        android:textSize="@dimen/text_size_large" />

    <TextView
        android:id="@+id/text_extra"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/view_medium_spacing"
        android:fontFamily="sans-serif-condensed-medium"
        android:gravity="center"
        android:text="@string/extra_splash_text"
        android:textAllCaps="true"
        android:textSize="@dimen/text_size_small" />

</RelativeLayout>